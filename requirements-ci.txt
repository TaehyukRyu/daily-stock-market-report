# requirements-ci.txt — 파이프라인 실행 전용 (테스트/UI 제외)
#
# [DEP-02] langchain-core를 최상단에 명시해 설치 순서 보장.
#          langchain-anthropic 미포함 → langchain-core 다운그레이드 위험 없음 (DEP-01).
#
# TA-Lib==0.4.32 는 C 라이브러리가 선행 설치돼야 함:
#   Ubuntu: sudo apt-get install -y libta-lib-dev
#   macOS:  brew install ta-lib

# ── LangChain 코어 (먼저 고정) ───────────────────────────────────────────────
langchain-core==1.3.3

# ── LLM API ──────────────────────────────────────────────────────────────────
anthropic==0.50.0
openai==1.75.0
tiktoken==0.12.0

# ── LangChain 스택 ───────────────────────────────────────────────────────────
langchain==0.3.25
langchain-openai==0.3.16
langchain-chroma==1.1.0
langchain-community==0.4.1
langchain-text-splitters==1.1.2
langsmith==0.8.2

# ── LangGraph ────────────────────────────────────────────────────────────────
langgraph==0.4.1
langgraph-checkpoint==2.1.2
langgraph-prebuilt==0.5.1
langgraph-sdk==0.3.12

# ── 벡터 DB / RAG ─────────────────────────────────────────────────────────────
chromadb==1.5.9
chroma-hnswlib==0.7.6
rank-bm25==0.2.2
kiwipiepy==0.23.1
kiwipiepy_model==0.23.0

# ── 한국/미국 시장 데이터 ────────────────────────────────────────────────────
pykrx==1.2.7
yfinance==1.3.0
fredapi==0.5.2
dart-fss==0.4.9

# ── 기술적 분석 (C 라이브러리 선행 설치 필요) ────────────────────────────────
TA-Lib==0.4.32
ta==0.11.0

# ── 데이터 처리 ─────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==2.4.4
scipy==1.17.1
statsmodels==0.14.4
matplotlib==3.10.8

# ── HTTP / 비동기 ────────────────────────────────────────────────────────────
aiohttp==3.11.18
aiofile==3.9.0
requests==2.33.0
httpx==0.28.1
beautifulsoup4==4.13.3

# ── MCP ──────────────────────────────────────────────────────────────────────
fastmcp==3.2.4
mcp==1.27.0

# ── 스케줄러 / 복원력 ────────────────────────────────────────────────────────
APScheduler==3.11.0
tenacity==9.1.4
pybreaker==1.4.1

# ── 설정 / 스키마 ─────────────────────────────────────────────────────────────
python-dotenv==1.2.2
pydantic==2.13.3
pydantic-settings==2.14.0

# ── 발행 ─────────────────────────────────────────────────────────────────────
notion-client==2.3.0
